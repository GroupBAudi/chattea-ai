# chattea-ai

1. Run

```python
pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu130
```

Or not your device doesn't have CUDA:

```python
pip install torch torchvision torchaudio
```

Then just:
```python
pip install -r requirements.txt 
```

Or something along the line. Watch as your space gets eaten alive (use .venv please 🥹)